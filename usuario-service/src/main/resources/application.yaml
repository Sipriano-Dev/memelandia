server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:file:./data/memelandia-db
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: validate   # 🔴 NÃO é update
    defer-datasource-initialization: false

  sql:
    init:
      mode: never          # 🔴 DESLIGADO

  h2:
    console:
      enabled: true
      path: /h2-console

